# Also load your local tools flake (only if it exists)
if [ -d "$HOME/flakes/repomix-tools" ]; then
    use flake "path:$HOME/flakes/repomix-tools"
fi

# Load this repo's dev shell last so its tool versions win on PATH
use flake .
